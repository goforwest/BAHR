# 🎭 BAHR Frontend — واجهة المستخدم

> واجهة حديثة مبنية على Next.js 16 + TypeScript + Tailwind CSS v4 + shadcn/ui مع دعم كامل للغة العربية والكتابة من اليمين لليسار (RTL)

## ✅ ما تم إنجازه

### 📦 التقنيات المستخدمة
- ✅ **Next.js 16.0.1** مع App Router
- ✅ **TypeScript** للأمان النوعي
- ✅ **Tailwind CSS v4** (أحدث إصدار)
- ✅ **shadcn/ui** للمكونات الجاهزة
- ✅ **دعم RTL كامل** بدون مكتبات إضافية
- ✅ **خطوط عربية** (Cairo للعناوين، Amiri للنصوص الشعرية)

### 🎨 الخطوط العربية
```typescript
// تحميل مُحسّن عبر next/font/google
const cairo = Cairo({
  variable: "--font-cairo",
  subsets: ["arabic"],
  display: "swap",
});

const amiri = Amiri({
  variable: "--font-amiri",
  weight: ["400", "700"],
  subsets: ["arabic"],
  display: "swap",
});
```

### 🔄 دعم RTL
- ✅ `<html lang="ar" dir="rtl">` في `layout.tsx`
- ✅ تهيئة Tailwind CSS v4 للاتجاه من اليمين لليسار
- ✅ صفحة رئيسية تجريبية بالكامل بالعربية

## 🚀 الأوامر

```bash
# تشغيل الخادم المحلي
npm run dev

# بناء الإنتاج
npm run build

# تشغيل بيئة الإنتاج
npm start

# فحص الأكواد
npm run lint
```

## 📂 هيكل المشروع

```
frontend/
├── src/
│   ├── app/
│   │   ├── globals.css       # تهيئة Tailwind + CSS Variables
│   │   ├── layout.tsx         # Layout رئيسي مع RTL + الخطوط
│   │   └── page.tsx           # الصفحة الرئيسية (أمثلة عربية)
│   └── lib/
│       └── utils.ts           # shadcn utilities (cn helper)
├── public/                    # ملفات ثابتة
├── components.json            # تهيئة shadcn/ui
├── next.config.ts             # تهيئة Next.js
├── tailwind.config.ts         # تهيئة Tailwind (v4)
├── tsconfig.json              # تهيئة TypeScript
└── package.json
```

## 🎯 الميزات المُنفَّذة

### 1. صفحة رئيسية عربية كاملة
- عنوان "بحر" بخط Cairo
- أمثلة شعرية بخط Amiri
- شبكة ميزات (كشف البحور، تحليل القوافي، التقطيع)
- شارة حالة مع رسوم متحركة

### 2. تصميم متجاوب (Responsive)
- يعمل على الهاتف، التابلت، والكمبيوتر
- `grid sm:grid-cols-3` للشبكات
- `flex-col` للعرض العمودي على الشاشات الصغيرة

### 3. دعم Dark Mode (جاهز)
- متغيرات CSS مهيأة في `globals.css`
- يمكن تفعيلها بإضافة `className="dark"` إلى `<html>`

## 🔧 إضافة مكونات shadcn/ui

```bash
# مثال: إضافة Button
npx shadcn@latest add button

# إضافة Card
npx shadcn@latest add card

# إضافة Form
npx shadcn@latest add form
```

## 📋 الخطوات القادمة

- [ ] إضافة صفحة تحليل الشعر
- [ ] ربط API مع Backend FastAPI
- [ ] إضافة نماذج إدخال للقصائد
- [ ] عرض نتائج التحليل (البحور، القوافي، التفعيلات)
- [ ] إضافة أمثلة تفاعلية
- [ ] تحسين SEO للمحتوى العربي

---

**🎉 النظام جاهز للتطوير!**

افتح المتصفح على: http://localhost:3000
